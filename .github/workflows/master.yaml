name: "Compliance333"

on:
  push:
  workflow_dispatch:

jobs:
  compliancess2:
    runs-on: ubuntu-latest
    strategy:
      matrix: 
        nomes: ['Enzo', 'Luciana', 'Vítor']
    steps:  
      
      - name: imprime
        with:
          names: ${{matrix.nomes}}
        run: echo "nome: ${{names}}"
