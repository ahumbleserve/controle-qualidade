name: Compliance
  on:
    workflow_dispatch:
jobs:
  semantics:
    name: Semantics
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: ahumbleserve/REST-Todo-API/.github/workflows/master.yaml@master

