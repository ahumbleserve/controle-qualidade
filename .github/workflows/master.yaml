name: Compliance

on:
  workflow_call:

permissions:
  pull-requests: write

jobs:
  semantics:
    name: Semantics
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: ahumbleserve/REST-Todo-API/.github/workflows/master.yaml@master

